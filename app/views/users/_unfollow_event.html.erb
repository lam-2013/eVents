<% # linked to the destroy action in the User_follow_locals controller %>
<%= form_for(current_user.partecipa_events.find_by_evento_id(@event), html: { method: :delete }, remote: true ) do |f| %>
    <%= f.submit 'chiudi evento', class: 'btn btn-large' %>
<% end %>